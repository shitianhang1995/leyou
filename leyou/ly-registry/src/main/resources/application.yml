server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    #不注册到注册中心去
    register-with-eureka: false
    #不拉取服务
    fetch-registry: false